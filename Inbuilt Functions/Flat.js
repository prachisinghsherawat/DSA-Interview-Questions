

// Flat() ------> Flat method creates a new array with all sub-array elements concatenated into it recursively up to the specified depth


// Flatten Array 

let arr = [1,2,[1,2,3,4],[[1,2]]];

console.log(arr.flat(2))


// OUTPUT is ----> [1,2,1,2,3,4,1,2]
